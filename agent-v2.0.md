# 决策番茄钟 | Decision Pomodoro — v2.0

## 项目概述

**决策番茄钟**是一个基于网页的专注工具，将经典番茄工作法与结构化决策流程相结合。它通过强制性的阶段拆分、产出要求和防发散机制，帮助用户在有限时间内做出高质量决策，避免"分析瘫痪"和过度发散。

**核心设计理念**："完成大于完美"——先产出最小可行结论，再迭代优化。

---

## 项目信息

| 属性 | 值 |
|------|-----|
| **项目名称** | 决策番茄钟决策系统 |
| **当前版本** | v2.0 |
| **创建日期** | 2025-06-11 |
| **项目路径** | `F:\hermess\projects\20250611-番茄钟决策系统` |
| **技术栈** | Vite + 纯前端（HTML + CSS + JavaScript），零运行时依赖 |
| **存储方式** | 浏览器 localStorage（本地持久化） |

---

## 版本体系

从 v2.0 起，项目使用 Vite 构建工具将源文件（`src/`）打包为单 HTML 文件。

### 版本映射

| 版本 | 技术方案 | 内容 |
|------|----------|------|
| v1.0 | 单 HTML | 初始版本：五阶段决策流程、防发散机制、ICS/Markdown 导出 |
| v1.1 | 单 HTML | + 时间自定义设置（配置档案系统，最多5个）+ 音效基础设施 |
| v1.2 | 单 HTML | + 木鱼音效系统（ConvolverNode 混响）+ Google Calendar 集成 + AI 辅助 |
| v1.3 | 单 HTML | + 产出类型胶囊按钮组 + placeholder 联动 |
| v1.4 | 单 HTML | + 轮次选择器（1轮/2轮）+ 蓝色色调优化 |
| v1.5 | 单 HTML | + 浏览器通知（阶段完成时桌面弹窗） |
| **v2.0** | **Vite 模块化** | 从单 HTML 重构为 Vite + 模块化 JS（timer/ui/sound/...），继承 v1.5 全部功能 |

---

## 文件结构

```
20250611-番茄钟决策系统/
├── decision-pomodoro/            # v2.0 项目（Vite 模块化）
│   ├── index.html                # HTML 入口
│   ├── package.json              # Vite 依赖配置
│   ├── public/favicon.svg        # 网站图标
│   └── src/                      # 源码目录
│       ├── main.js               # 入口：初始化 + 事件绑定
│       ├── timer.js              # 计时器 + 阶段管理
│       ├── ui.js                 # UI 渲染函数
│       ├── state.js              # 状态管理（可导出变量 + setter）
│       ├── config.js             # 配置存储与读取
│       ├── sound.js              # 木鱼音效合成（Web Audio API）
│       ├── storage.js            # localStorage 持久化
│       ├── export.js             # ICS / Markdown 导出
│       ├── settings.js           # 时间设置弹窗逻辑
│       ├── ai.js                 # AI 助手（OpenAI 兼容 API）
│       └── style.css             # 全部样式
├── 启动番茄钟.bat                 # 一键启动 Vite 开发服务器
├── CLAUDE.md                     # 项目说明
├── CHANGELOG.md                  # 版本日志
└── archives/                     # 旧版本存档（v1.0 ~ v1.5）
    ├── pomodoro-v1.1.html
    ├── pomodoro-v1.2.html
    ├── pomodoro-v1.3.html
    ├── pomodoro-v1.4.html
    ├── pomodoro-v1.5.html
    ├── agent-v1.1.md
    ├── agent-v1.2.md
    ├── agent-v1.3.md
    ├── agent-v1.4.md
    ├── agent-v1.5.md
    └── 启动番茄钟-v1.0.md
```

---

## 核心功能

### 五阶段决策流程（支持 1 轮或 2 轮）

| 阶段 | 图标 | 时长 | 核心任务 | 强制产出 |
|------|------|------|----------|----------|
| **🧩 问题拆分** | 🧩 | 可调 | 将大问题拆分为"今天能完成的、可判断标准"的小任务 | 具体下一步动作（必须填写才能开始） |
| **🎯 执行轮次1** | 🎯 | 可调 | 只做拆分好的任务，不想别的 | 执行产出（必须填写才算完成） |
| **📝 复盘轮次1** | 📝 | 可调 | 是否达到完成标准？原因是什么？ | 复盘笔记 |
| **🎯 执行轮次2** | 🎯 | 可调 | 继续深入或调整方向 | 执行产出（必须填写）|
| **📝 复盘轮次2** | 📝 | 可调 | 输出主结论/概念卡/证据表/最小行动 | 最终复盘 + 最弱结论（如未达标）|

> 执行轮次2 和复盘轮次2 仅在 2 轮模式下出现。1 轮模式下流程为：拆分 → 执行 → 复盘。

**默认时长**：65分钟（2轮: 5+25+5+25+5），可通过时间设置自定义（支持保存最多5套配置，每套可选1轮或2轮）

### 防发散机制

1. **拆分阶段强制产出**：必须先写出"具体下一步动作"才能开始计时
2. **执行阶段强制产出**：没有产出不算完成一轮
3. **10分钟走神检查**：执行阶段10分钟时弹出确认弹窗——"还在主线上吗？"
4. **卡住处理**：提供三种应对策略模板（拆小、换角度、最小行动）
5. **强制止损**（仅2轮模式）：连续两轮未达标时强制输出"最弱结论"并结束

### 辅助功能

| 功能 | 说明 |
|------|------|
| **💭 想法拦截** | 执行中突然想到其他事？记录下来继续执行，不中断当前流程 |
| **📋 今日记录** | 自动保存每轮次的阶段产出、时间、任务名 |
| **📅 导出ICS** | 将记录导出为 `.ics` 文件，拖入 Google Calendar 即可导入 |
| **📝 导出Markdown** | 自动生成结构化的 Markdown 报告，含阶段表格、复盘笔记、完成情况 |
| **⏸ 暂停/继续** | 随时暂停，恢复后继续计时 |
| **🏁 提前结束** | 可手动结束本轮，已完成的阶段会保存 |
| **⚙️ 时间自定义** | 支持保存最多5套时间配置，每套可选1轮或2轮，随时切换 |
| **🔊 音效提醒** | Web Audio API 木鱼音色合成（ConvolverNode 混响空灵版），音量可调 |
| **📅 Google Calendar** | 一键打开 Google Calendar 预填事件页 + 下载 .ics 文件 |
| **🤖 AI 助手** | OpenAI 兼容 API，拆分阶段帮拆任务、复盘阶段帮总结 |
| **🔔 浏览器通知** | Notification API，阶段完成时系统级桌面弹窗 |

---

## 启动方式

### 方式一：双击批处理文件（推荐）
双击 `启动番茄钟.bat`，自动启动 Vite 开发服务器并在浏览器中打开。

### 方式二：手动启动
```bash
cd "F:\hermess\projects\20250611-番茄钟决策系统\decision-pomodoro"
npx vite
```

### 方式三：构建后直接打开 HTML
```bash
cd "F:\hermess\projects\20250611-番茄钟决策系统\decision-pomodoro"
npm run build
# 打开 dist/index.html
```

---

## 开发说明

```bash
cd decision-pomodoro
npm run dev      # 开发模式（热更新）
npm run build    # 构建生产版本到 dist/
```

所有源代码在 `src/` 目录下，按功能模块分离：

| 模块 | 职责 |
|------|------|
| `main.js` | 应用初始化、事件绑定 |
| `config.js` | 配置档案存储与读取 |
| `state.js` | 全局状态管理 |
| `timer.js` | 计时器引擎、阶段状态机 |
| `ui.js` | DOM 渲染函数 |
| `sound.js` | Web Audio API 木鱼音效 |
| `storage.js` | localStorage 数据持久化 |
| `export.js` | ICS / Markdown 导出 |
| `settings.js` | 设置弹窗逻辑 |
| `ai.js` | AI 助手 |

---

## 维护说明

- **版本管理**：每完成一个功能，更新 `CHANGELOG.md` 并更新版本号
- **无需旧版归档**：旧版本已存档至 `archives/`
- **数据备份**：F12 → Application → Local Storage → 复制 `pomodoro_YYYY-MM-DD` 键值

---

*最后更新：2026-06-16*
*当前版本：v2.0 — "完成大于完美"*
